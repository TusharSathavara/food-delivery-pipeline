# Task 1 — Repository setup and assessment Git workflow
mkdir food-delivery-pipeline
cd food-delivery-pipeline
git init
mkdir src data data/raw data/processed docs tests
type nul > src/.gitkeep
type nul > data/raw/.gitkeep
type nul > data/processed/.gitkeep
type nul > docs/.gitkeep
type nul > tests/.gitkeep
git add .
git commit -m "feat: initialise project structure"

# Add README and .gitignore
git add README.md .gitignore
git commit -m "docs: add README and .gitignore"

# Add Task 2
git add src/task2_cleaning.py
git commit -m "feat: add restaurant cleaning script"

# Add Task 3
git add src/task3_fee_calculator.py
git commit -m "feat: add functional fee calculator"

# Add Task 4
git add src/task4_record_manager.py
git commit -m "feat: add record manager with JSON persistence"

# Feature branch for capstone
git checkout -b feature/live-data-pipeline
git add src/food_delivery_pipeline.py requirements.txt tests/test_pipeline.py
git commit -m "feat: add live data pipeline console tool"

# Merge feature branch into main locally
git checkout main
git merge --no-ff feature/live-data-pipeline -m "merge: live data pipeline feature"

# Push the repository
git branch -M main
git remote add origin <YOUR_GITHUB_REPOSITORY_URL>
git push -u origin main

# If the assessor requires a visible GitHub Pull Request:
# Push the feature branch before merging on GitHub:
git checkout feature/live-data-pipeline
git push -u origin feature/live-data-pipeline
# Open a GitHub Pull Request from feature/live-data-pipeline -> main,
# review it, merge it, then pull the updated main branch.
